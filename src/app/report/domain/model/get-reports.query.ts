/**
 * Query object for retrieving reports in the domain layer.
 * @remarks
 * Encapsulates criteria for fetching reports. Currently retrieves all reports for the authenticated user,
 * but can be extended to include filters (date range, type, etc.).
 * @author FloweyTech developer team
 */
export class GetReportsQuery {
  constructor() {
  }
}
